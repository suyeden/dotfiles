# Emacs

## Requirements

- Node.js
- npm
- SBCL (for Common Lisp / SLIME)

## Setup

``` bash
npm install -g \
  typescript \
  typescript-language-server \
  vscode-langservers-extracted \
  prettier
```
